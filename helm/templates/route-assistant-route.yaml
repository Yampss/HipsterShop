apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ .Values.routes.assistant.name }}
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ .Values.assistantservice.name }}
    app.kubernetes.io/name: hipster-shop
    app.kubernetes.io/component: route
spec:
  parentRefs:
    - name: {{ .Values.gateway.name }}

  rules:
    - matches:
        - path:
            type: Exact
            value: {{ .Values.routes.assistant.path }}

      backendRefs:
        - name: {{ .Values.assistantservice.name }}
          port: {{ .Values.assistantservice.service.port }}
