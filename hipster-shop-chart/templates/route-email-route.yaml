apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ .Values.routes.email.name }}
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ .Values.emailservice.name }}
spec:
  parentRefs:
    - name: {{ .Values.gateway.name }}

  rules:
    - matches:
        - path:
            type: Exact
            value: {{ .Values.routes.email.path }}

      filters:
        - type: URLRewrite
          urlRewrite:
            path:
              type: ReplaceFullPath
              replaceFullPath: {{ .Values.routes.email.rewrite }}

      backendRefs:
        - name: {{ .Values.emailservice.name }}
          port: {{ .Values.emailservice.port }}

