apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ .Values.routes.payment.name }}
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ .Values.paymentservice.name }}
spec:
  parentRefs:
    - name: {{ .Values.gateway.name }}

  rules:
    - matches:
        - path:
            type: Exact
            value: {{ .Values.routes.payment.path }}

      filters:
        - type: URLRewrite
          urlRewrite:
            path:
              type: ReplaceFullPath
              replaceFullPath: {{ .Values.routes.payment.rewrite }}

      backendRefs:
        - name: {{ .Values.paymentservice.name }}
          port: {{ .Values.paymentservice.port }}

