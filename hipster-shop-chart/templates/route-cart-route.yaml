apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ .Values.routes.cart.name }}
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ .Values.cartservice.name }}
spec:
  parentRefs:
    - name: {{ .Values.gateway.name }}

  rules:
    - matches:
        - path:
            type: PathPrefix
            value: {{ .Values.routes.cart.path }}

      filters:
        - type: URLRewrite
          urlRewrite:
            path:
              type: ReplacePrefixMatch
              replacePrefixMatch: {{ .Values.routes.cart.rewrite }}

      backendRefs:
        - name: {{ .Values.cartservice.name }}
          port: {{ .Values.cartservice.port }}


